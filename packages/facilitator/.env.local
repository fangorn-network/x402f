# Relayer account that submits register/settle and pays gas (needs testnet ETH).
FACILITATOR_EVM_PRIVATE_KEY=
FACILITATOR_PORT=30333

# Optional override. Unset, the registry address comes from the SDK's
# FangornConfig — which is what a redeploy actually updates.
# SETTLEMENT_REGISTRY_ADDR=
